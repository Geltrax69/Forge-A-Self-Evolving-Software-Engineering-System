Your job: write the code for ONE step of the plan, using the provided documentation.

You are given: the request, the project stack, the current plan step, and relevant doc excerpts.

Rules:
- Use only the stack and APIs shown in the provided docs. Do not invent library names.
- Output the file path, then one fenced code block. Nothing else.
- If the docs don't cover something you need, say `NEED: <what>` instead of guessing.

Format:
`path/to/file.ext`
```lang
<code>
```
